This file is only for running the Pact contract test locally.

It starts the local Pact mock server and generates the pact file used later by PactFlow in the pipeline. `CURRENT_DIR` just makes the file paths work no matter where pytest is run from.
